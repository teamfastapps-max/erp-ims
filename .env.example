# ─────────────────────────────────────────────
# Dev server (docker-compose.yml - connects to existing hms-db on same host)
# ─────────────────────────────────────────────
VIMS_IMAGE=304152263294.dkr.ecr.ap-south-1.amazonaws.com/vims-dev:latest
VIMS_HTTP_PORT=81

# AWS credentials are configured on the server via IAM role or aws configure -- not needed here
